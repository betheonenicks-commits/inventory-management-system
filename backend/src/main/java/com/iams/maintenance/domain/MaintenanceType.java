package com.iams.maintenance.domain;

/** US-LIF-07/08: preventive (scheduled) vs corrective (unscheduled) maintenance - kept distinct per AC-LIF-08-H. */
public enum MaintenanceType {
    PREVENTIVE,
    CORRECTIVE
}
