package com.iams.inventory.domain;

/** US-INV-11: the fixed set FR-INV-11's own examples name (each/box/kg/litre), plus two common bulk-stock units. */
public enum UnitOfMeasure {
    EACH,
    BOX,
    KG,
    LITRE,
    PACK,
    ROLL
}
