package com.iams.analytics.domain;

/** US-ANL-04: the quick-pick categories. Category alone is accepted - it's still useful signal per the AC. */
public enum FeedbackCategory {
    BUG,
    IDEA,
    FRICTION,
    PRAISE,
    OTHER
}
