package com.iams.audit.domain;

/** US-AUD-10: the fixed condition scale a finding's condition value must fall on. */
public enum AssetCondition {
    GOOD,
    FAIR,
    MINOR_DAMAGE,
    MAJOR_DAMAGE,
    UNUSABLE
}
